<?xml version="1.0" encoding="utf-8"?>
<!-- Custom transport controls so the Sources button can sit alongside the
     subtitle and settings buttons instead of floating over the video. -->
<!-- No tint across the picture. Showing the controls used to darken the whole
     screen, and the controls are shown by pausing — which is exactly what
     somebody does to read a subtitle they could not keep up with. The shading
     now follows the buttons rather than covering the video. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="horizontal"
        android:background="@drawable/control_pill"
        android:paddingStart="8dp"
        android:paddingEnd="8dp">

        <!-- Custom ids, not exo_prev/exo_next: those are wired to playlist
             navigation, and there is no playlist — each episode is resolved
             on demand. -->
        <ImageButton
            android:id="@+id/prev_episode"
            style="@style/ExoStyledControls.Button.Center.Previous"
            android:contentDescription="Previous episode" />

        <Button
            android:id="@+id/exo_rew_with_amount"
            style="@style/ExoStyledControls.Button.Center.RewWithAmount" />

        <ImageButton
            android:id="@+id/exo_play_pause"
            style="@style/ExoStyledControls.Button.Center.PlayPause" />

        <Button
            android:id="@+id/exo_ffwd_with_amount"
            style="@style/ExoStyledControls.Button.Center.FfwdWithAmount" />

        <ImageButton
            android:id="@+id/next_episode"
            style="@style/ExoStyledControls.Button.Center.Next"
            android:contentDescription="Next episode" />
    </LinearLayout>

    <!-- Subtitles, cast, sources, settings and the scrubber all carry the same
         shading, for the same reason: white on bare video disappears against a
         bright scene. It is kept to the block the controls occupy, which sits
         below the subtitles rather than over them. -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:background="@drawable/control_pill"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="12dp"
        android:layout_marginBottom="12dp"
        android:paddingTop="4dp"
        android:paddingBottom="16dp">

        <!-- Track and source controls sit bottom-right, above the scrubber. -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="end|center_vertical"
            android:orientation="horizontal"
            android:paddingEnd="8dp"
            android:paddingStart="8dp">

            <ImageButton
                android:id="@+id/exo_subtitle"
                style="@style/ExoStyledControls.Button.Bottom.CC" />

            <ImageButton
                android:id="@+id/cast_button"
                android:layout_width="52dp"
                android:layout_height="52dp"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:contentDescription="Cast"
                android:padding="14dp"
                android:src="@drawable/ic_cast" />

            <ImageButton
                android:id="@+id/sources_button"
                android:layout_width="52dp"
                android:layout_height="52dp"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:contentDescription="Sources"
                android:padding="14dp"
                android:src="@drawable/ic_sources" />

            <ImageButton
                android:id="@+id/exo_settings"
                style="@style/ExoStyledControls.Button.Bottom.Settings" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingStart="16dp"
            android:paddingEnd="16dp">

            <TextView
                android:id="@+id/exo_position"
                style="@style/ExoStyledControls.TimeText.Position" />

            <androidx.media3.ui.DefaultTimeBar
                android:id="@+id/exo_progress"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="26dp"
                app:played_color="#FF8B5CF6"
                app:scrubber_color="#FF8B5CF6"
                app:buffered_color="#552A2140"
                app:unplayed_color="#552A2140" />

            <TextView
                android:id="@+id/exo_duration"
                style="@style/ExoStyledControls.TimeText.Duration" />
        </LinearLayout>
    </LinearLayout>

</FrameLayout>
